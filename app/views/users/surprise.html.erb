<div style="margin-bottom:150px">
	<div style="width:100px"><button style="margin-bottom:10px" type="button" class="btn btn-default">一等奖</button>
	<button style="margin-bottom:10px" type="button" class="btn btn-default">二等奖</button>
	<button  type="button" class="btn btn-default">三等奖</button>
</div>
</div>
<div class="table-responsive" style="height:400px;">

	<table class="table">		
			<% @users.each do |user| %>
			<tr>
        <td class="active" style="width:200px"><%= user.name %></td>
        <td><%= image_tag(user.picture_url, :width =>100, :height=>100) if user.picture.present?%></td>
        </tr>
			<% end %>
	</table>

</div>